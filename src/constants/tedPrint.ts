/** Ancho útil impresora térmica 58 mm (~384 dots a 203 dpi). */
export const TED_PRINT_WIDTH = 384;

/** Altura máxima del PDF417 en el recibo (evita estirar el timbre). */
export const TED_PRINT_MAX_HEIGHT = 96;
