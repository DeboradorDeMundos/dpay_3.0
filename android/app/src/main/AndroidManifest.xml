<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Declaración de visibilidad para apps de Tuu (Android 11+) -->
    <queries>
        <!-- TUU Demo - App de pruebas para desarrollo -->
        <package android:name="com.haulmer.demo" />
        <!-- TUU Pagos DEV - Requiere dispositivo registrado -->
        <package android:name="com.haulmer.paymentapp.dev" />
        <!-- TUU Pagos PROD - Producción -->
        <package android:name="com.haulmer.paymentapp" />
        <!-- Tienda TUU / Xcheng App Store (actualizaciones D-PAY) -->
        <package android:name="com.xcheng.store" />
        <package android:name="com.pos.service" />
        <!-- Abrir URLs https en el navegador del sistema -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Abrir URLs http en el navegador del sistema -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- Deep link tienda Xcheng/TUU -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tms" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permisos Bluetooth para impresoras -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- neverForLocation: permite descubrir dispositivos sin requerir permiso de ubicación en runtime (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    
    <!-- Permisos de ubicación necesarios para escaneo Bluetooth en Android -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Permisos para almacenamiento de PDFs (solo Android 9 e inferior).
         Android 10+ usa almacenamiento con alcance (scoped storage): el directorio
         privado de la app (getExternalCacheDir) no requiere estos permisos. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    
    <!-- Permisos para ejecución en segundo plano -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Serial del hardware POS (Payment Hub / enrolamiento) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- SDK Kozen/Xcheng POI (lectura TUSN/serial vía com.pos.sdk) -->
    <uses-permission android:name="com.pos.permission.SECURITY" />

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Ajustar volumen media si está en 0 al escanear (beep POS) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Feedback háptico al escanear producto -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:supportsRtl="true"
      tools:replace="android:appComponentFactory"
      android:appComponentFactory="androidx.core.app.CoreComponentFactory">
      <uses-library android:name="com.pos.sdk" android:required="false" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
        <service 
            android:name="com.asterinet.react.bgactions.RNBackgroundActionsTask" 
            android:foregroundServiceType="dataSync"
            android:exported="false" />
    </application>
</manifest>
