# Ignorar conflictos de peer dependencies (solo de herramientas de desarrollo)
legacy-peer-deps=true

# Usar lockfile exacto
package-lock=true

# Guardar versiones exactas (sin ^ ni ~)
save-exact=true
